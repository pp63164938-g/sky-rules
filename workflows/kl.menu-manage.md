---
trigger: /kl.menu-manage
description: Kunlun 自动管理菜单与权限，无需操作页面，直接调用 API。
---

# 菜单与权限管理自动化技能 (Menu Manage)

本工作流旨在让 AI 完全自动化地向 `skyline-kunlun` 项目插入/修改系统菜单和权限按钮，替代繁琐的页面填表操作。

## 工作流规则与执行步骤

当用户使用 `/kl.menu-manage` 并附带操作指令（如“新增一个菜单”或“添加一个按钮”）时，你（AI）**必须**严格遵循以下执行流程：

### 1. 凭证解析与参数提取
- **获取凭证**：检查对话上下文中是否包含用户从浏览器复制的请求报文（cURL 或 raw HTTP）。从中提取出 `Authorization` Token（通常以 `Bearer ` 开头）以及请求的域名 `BASE_URL`。如果没有发现有效凭证，**必须中断执行**，向用户索要任意一个带有 Token 的请求报文。
- **确认参数**：根据用户的口语描述，提取或推断以下必填字段。如果不明确，需主动向用户确认：
  - `parentRouter` (优先) 或 `parentName`: 父菜单的前端路由路径（例如 `/businessOperations/public/worldcup`）或中文名称（用于去树结构中精准反查 `parentId`）。使用路由定位是最准的！
  - `name`: 即将创建的 菜单/按钮名称
  - `international`: 唯一标识（即权限 Key）。
    **【核心一致性要求】**：此标识必须与前端 Vue 代码中 `useCustomizeField('唯一标识', ...)` 的参数**保持绝对一致**！如果是 Tab 页面，则必须取对应 Tab 的唯一标识，以保证全局唯一。
    **【先确定，再双端同步】**：在执行菜单自动化脚本或修改代码前，**必须先确定好这个唯一的 `international`**。一旦确定，必须将其**同时应用**于后端菜单生成脚本的 `international` 字段，以及前端代码的 `v-sky-authorize` 属性中（一起做这两个事情）。绝不能出现前后端两套标识导致不同步的问题！
    （**注意**：如果是该页面下的导出按钮，必须强制在其父级标识后统一使用 `:export` 作为后缀，如 `...:tab-registration:export`）。
  - `router`: 前端页面路由路径（如果是按钮，可以填写空字符串 `""`）

#### 菜单表单必填字段硬校验

- **写入前必须校验菜单表单必填字段**：新增或更新菜单 / 按钮资源前，必须按当前工程菜单表单真实字段构造 payload，并逐项校验必填字段。任何必填字段缺失、为 `undefined`、为 `null`，或为不允许的空字符串时，必须中断执行并让用户补充，禁止提交接口。
- **字段名必须跟随当前工程**：必须以 `src/components/sys/src/menu/sys-dialog-menu.vue` 等当前工程源码为准，使用真实字段名，例如 `devVersion`、`mobileFlag`，禁止自行使用 `version`、`appShowFlag` 等非当前工程字段名。
- **新增资源必填字段**：`resourceType`、`name`、`international`、`parentId`、`router`、`devVersion`、`sort`、`webShowFlag`、`mobileFlag`。按钮资源的 `router` 允许为空字符串 `""`；菜单资源的 `router` 不允许为空。
- **更新资源额外必填字段**：除新增资源必填字段外，必须包含 `id`。
- **允许空值必须显式说明**：如 `icon` 可为空字符串、`interfaceList` 可为空数组，但必须仍按当前工程 payload 带上，禁止传 `null`。
- **预览表必须展示所有必填字段**：提交前的确认预览必须展示 `devVersion`，不能只展示业务名称和权限标识；发现版本号缺失时必须标为“缺失，待用户补充”，不能用空值提交。

#### 父级展示硬性要求

- 所有预览表和执行结果表中的“父级”禁止只展示 `parentId`。
- 必须同时展示父级菜单名称、父级完整面包屑路径、父级 ID，以及父级 `international`（能查到时必须展示）。
- 如果父级是本次批量新增中的新菜单，预览时必须展示为“待创建：父级菜单名称（创建后作为下级资源父级）”，并在备注中说明真实 ID 将在创建后回填。
- 如果无法解析父级名称或完整面包屑路径，必须停止在预览前，继续查树 / detail / list；禁止退化成只展示 ID。

### 1.1 权限点自动发现模式

当用户提供截图、当前打开文件、Vue 文件路径、目录路径、路由路径或菜单名称，并要求“梳理权限 / 生成权限 / 替换标识 / 同步菜单权限”时，必须进入权限点自动发现模式。

#### 输入解析

- 用户提供截图时：先识别截图中的菜单文案，作为 `parentName` 候选。
- 用户提供 Vue 文件路径或当前打开文件时：以该文件为入口，向上识别页面目录、`index.vue`、关联路由与菜单名称。
- 用户提供目录路径时：优先查找目录下的 `index.vue`，再搜索同目录及子目录中的业务按钮。
- 用户提供路由路径时：优先在 `router/**/*.ts` 中反查页面组件路径与菜单标题。
- 用户只提供菜单名称时：必须调用资源树接口查找真实菜单节点，不能凭名称拼权限标识。

#### 自动发现范围

必须扫描并输出以下权限相关点位：

1. 页面级唯一标识
   - `useCustomizeField('...')`
   - `useField('...')`
   - 路由对应的菜单资源 `international`
   - 若前端唯一标识与资源树父级 `international` 不一致，必须列为“需要替换”。

2. 按钮级权限
   - `v-sky-authorize`
   - `sky-authorize`
   - `el-button`
   - `com-download`
   - `com-link-button`
   - 带 `data-testid` 且触发业务动作的按钮或链接
   - 若业务按钮缺少权限控制，必须列为“需要补充”。

3. 子页面 / 详情页权限
   - 通过路由 `activeRouter`
   - 当前菜单下的 `detail` / `apply` / `score-adjust-detail` 等子路由
   - 同一业务页面内的提交、审批、查看审批详情等操作按钮

4. 弹窗内部按钮
   - 普通取消 / 确定按钮默认不生成菜单权限
   - 若按钮触发独立业务动作，如导入、导出、生成、取消、提交、审批、批量操作，必须列入权限点。

#### 标识确定规则

- 页面级标识必须以资源树中真实菜单节点的 `international` 为准。
- 按钮级标识必须基于父级菜单 `international` 添加后缀。
- 已存在按钮资源时，必须复用接口返回的真实 `international`。
- 不存在按钮资源时，只能生成“待创建标识”，并在预览中标明将通过接口新增。
- 禁止仅凭文件路径、`data-testid` 或按钮文案静默推断为已存在权限。

#### 常用按钮后缀约定

| 按钮语义 | 后缀 |
| --- | --- |
| 添加 / 新增 | `:add` |
| 编辑 | `:edit` |
| 删除 | `:delete` |
| 复制 | `:copy` |
| 导出 | `:export` |
| 导入 | `:import` |
| 查看详情 | `:detail` |
| 提交 | `:submit` 或按业务使用 `:submit-score` |
| 生成任务 | `:generate-task` |
| 取消任务 | `:cancel-task` |
| 调整得分 | `:adjust-score` |

如果按钮语义无法落入已有后缀，必须在预览中标为“待确认后缀”，禁止直接写入代码或创建资源。

#### 必须输出的确认预览

执行任何代码替换或接口写入前，必须输出以下内容让用户确认：

1. 菜单资源现状表
   - 菜单名称
   - 完整面包屑路径
   - 菜单 ID
   - 资源树 `international`
   - 当前已有按钮资源

2. 前端权限点表
   - 文件路径
   - 行号
   - 按钮 / 功能名称
   - 当前标识
   - 目标标识
   - 处理动作：保留 / 替换 / 新增权限指令 / 待确认

3. 将要新增的菜单按钮资源表
   - 父级完整路径
   - 父级菜单
   - 父级 ID
   - 父级 `international`
   - 按钮名称
   - `international`
   - `resourceType`
   - `router`
   - `devVersion`
   - `sort`
   - `webShowFlag`
   - `mobileFlag`
   - 处理动作：待新增 / 已存在复用 / 待确认 / 阻断

   **表格格式要求**：必须使用表格，不得使用 bullet list。表格至少包含：

   | 资源名称 | 资源类型 | 父级完整路径 | 父级名称 | 父级 ID | 父级 international | international | router | devVersion | sort | webShowFlag | mobileFlag | 处理动作 |
   | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
   | xxx | 菜单 / 按钮 | 模块 / 一级菜单 / 二级菜单 | xxx | 123 | xxx | xxx | xxx | vX.X.X | 10 | 显示 | 隐藏 | 待新增 |

   说明：
   - `父级完整路径` 必须是类似“模块 / 一级菜单 / 二级菜单”的可读路径。
   - `父级名称` 和 `父级 ID` 必须拆列展示，不能只把 ID 塞进“父级”列。
   - `处理动作` 必须标明资源是待新增、已存在复用、待确认还是阻断。

4. 前端代码片段预览
   - 展示将要替换后的 `v-sky-authorize`
   - 展示将要替换后的 `useCustomizeField` / `useField`

必须询问用户：“请确认 Vue 代码的权限标识与将要生成的菜单配置是否一致并可以执行？”。用户确认前禁止修改代码、禁止调用新增资源接口。

#### 确认后的执行顺序

用户确认后必须按顺序执行：

1. 用 `apply_patch` 精准替换前端权限标识。
2. 生成临时 Node 脚本调用资源接口新增按钮。
3. 脚本必须先查资源树去重，已存在的按钮跳过，不重复创建。
4. 接口执行完成后删除临时脚本。
5. 再次调用资源树接口复查新增结果。
6. 搜索确认无残留：
   - `TODO：功能权限待配置`
   - `TODO待联调_权限标识`
   - `TODO:菜单管理页面【唯一标识】`
   - 与资源树不一致的旧页面标识
7. 对修改的 Vue 文件执行 SFC 编译解析和 `git diff --check`。
8. 最终同步输出必须使用表格：
   - 菜单资源生成结果表
   - 前端权限同步结果表
   - 是否仍有待确认权限点
   - 校验结果

#### 执行后结果回执表格式要求

菜单 / 权限资源写入完成后，最终回复必须输出“菜单资源生成结果表”，禁止只用 bullet list 粗略列出 ID。

执行后必须重新调用资源查询接口复查，最终表格以复查结果为准，而不是只以新增接口返回值为准。表格至少包含：

| 结果 | 资源名称 | 资源类型 | 资源 ID | 完整面包屑路径 | 父级名称 | 父级 ID | international | router | devVersion | sort | webShowFlag | mobileFlag |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 新增成功 / 已存在复用 / 新增后复查成功 / 跳过 / 失败 | xxx | 菜单 / 按钮 | 123 | 模块 / 一级菜单 / 二级菜单 / xxx | xxx | 123 | xxx | xxx | vX.X.X | 10 | 显示 | 隐藏 |

- 如果新增接口超时但复查发现资源已创建，应展示为“新增后复查成功”，并说明接口响应异常但资源树已确认落库。
- 如果某项失败，必须在表格后说明失败原因、影响范围和是否已经回滚 / 是否需要人工处理。

如果本次同时替换 Vue 代码权限，最终回复必须额外输出“前端权限同步结果表”：

| 文件 | 位置 / 功能 | 原标识 | 新标识 | 处理结果 |
| --- | --- | --- | --- | --- |
| xxx.vue | xxx 按钮 | xxx | xxx | 已替换 / 已新增 / 保留 / 跳过 |

禁止只说“已替换前端权限”。

### 2. 前端代码严格核验 (必须执行)
在提供预览和生成菜单前，**必须主动检查并确保**相关的 Vue 页面代码已经完成对应的权限配置同步：
- **检查权限指令**：确认目标按钮或区块上已经加上了正确的 `v-sky-authorize` 属性，且值与你拟定的标识后缀一致。
- **检查自定义列标识**：全局搜索或检查该页面下的 `useCustomizeField` Hook，确保不存在残留的 `TODO待联调_` 占位符，且其传入的唯一标识参数与父级页面的 `international` 保持绝对一致。
- **展示代码现状**：在最终发给用户的预览内容中，必须包含你检查或修改好的 Vue 代码片段供用户二次核对。

### 3. 前置查树与真实父级完整路径确认 (核心体验，必须执行)
在向用户提供预览前，**你（AI）必须先利用用户的 Token 编写一个查树脚本**，调用后端的 `/v2/sys/staff/resource/query/tree` 接口。
- **递归查找完整路径**：在拿到树结构数据后，必须在脚本中进行递归遍历，找到目标父级节点，并拼装出它的**完整菜单面包屑路径**（如 `运营中心 / 公共 / 世界杯活动管理 / 消费返现记录`）及其真实 ID。
- **必须依赖接口，非依赖页面**：因为用户需要核对按钮最终挂载的系统绝对位置，仅仅给出代码标识是没有业务意义的。
- **强制表单级全字段预览 (双端对齐)**：为了给用户提供最真实、最有安全感的确认体验，**必须完全复刻系统界面的填表项**进行展示。除了父级菜单作为挂载基准放在顶部外，其余所有表单项**必须全部塞进表格列中**（即使每行数据重复也要展示，主打绝对对齐）：
  
  **【父级菜单】** `运营中心 / 公共 / ... / 消费返现记录` (真实 ID: 6511xxxx)
  
  | 菜单名称 | 菜单类型 | 唯一标识 (international) | 菜单路径 (router) | 版本号 (devVersion) | 排序 | 是否显示 | 移动端显示 (mobileFlag) |
  | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
  | 导出 | 按钮(1) | `oc:xxx:xxx:export` | *(按钮为空)* | `v4.678.0` | 10 | 显示 | 隐藏 |

- **强制停顿 (严禁先斩后奏)**：展示**前端代码片段**和**表格预览**后，你**必须主动询问**用户：“请确认 Vue 代码的权限标识与将要生成的菜单配置是否一致并可以执行？”。**绝对禁止**在用户点头前去提前修改 Vue 代码或执行后端写入脚本！

### 4. 自动生成临时脚本 (Node.js)
在 `.gemini/antigravity/brain/<conversation-id>/scratch/` 目录下（使用绝对路径），利用 `write_to_file` 创建一个名为 `run-menu.js` 的脚本文件。脚本逻辑需包含：
- **第一步：查树取 ID**
  - **✅ 优先使用现成 ID (极速模式 O(1))**：**【核心避坑】**如果你在用户提供的 cURL 或请求报文中发现了 `parentId: xxxx` 的参数，**必须直接提取并使用该 `parentId`** 去调用 `POST {BASE_URL}/v2/sys/staff/resource/query/list` 进行精准操作。**严禁**无视现成参数去拉取包含几千个节点的 `/query/tree` 全量树做暴力遍历，否则会导致脚本执行时间从几秒飙升到几分钟的性能灾难！
  - **⚠️ 按路由找 ID (遍历模式 O(N))**：仅在没有任何 `parentId` 线索时，才发送 `GET {BASE_URL}/v2/sys/staff/resource/query/tree` 获取全量树。
  **【避坑指南/经验总结】**：`/query/tree` 接口**不返回 `router` 字段**。因此，当试图通过 `parentRouter` 查找父节点时，**必须**先递归提取树结构中的所有 `id`，然后分批并发请求 `GET {BASE_URL}/v2/sys/staff/resource/detail?id={id}`，遍历匹配详情里的 `router`，直到找到与 `parentRouter` 完全一致的节点，从而获取精准的 `parentId`。
- **关于删除节点的巨坑 (`/delete` 接口 Hang 死问题)**：
  **【避坑指南/经验总结】**：Kunlun 的 `/v2/sys/staff/resource/delete` 接口存在服务端 Bug。当 Node.js 调用该接口时，**服务端虽然能成功删除数据，但不会正确结束 HTTP 响应**，导致 Node.js 的请求被无限期挂起 (Hang 住) 并死锁。
  **解决方案**：如需在脚本中自动删除菜单，**必须为 HTTP 请求设置极短的超时时间（如 1000ms），并在 catch 中忽略超时报错**，只要发出了请求即认为成功，绝对不能使用 `await fetch` 傻等结果，否则整个自动化流程会彻底卡死。
- **第二步：执行写入**
  写入前必须按“菜单表单必填字段硬校验”逐项校验 payload；缺少任何必填字段时，必须中断脚本并向用户说明缺失字段，禁止继续提交接口。
  构造如下结构（`BodyCreateResource`），并通过 `POST {BASE_URL}/v2/sys/staff/resource/add` 发送：
  ```javascript
  {
      resourceType: "0", // 菜单为 "0"，按钮为 "1"
      name: "XXX",       // 提取的名称
      international: "XXX", 
      parentId: parentId, // 第一步查出来的真实 ID
      router: "XXX",     // 按钮通常为空字符串 ""
      devVersion: "vX.X.X", // 提取的版本号
      sort: 10,          // 默认均写 10，无需刻意递增
      webShowFlag: true,
      mobileFlag: false
  }
  ```
- **脚本输出字段必须可支撑最终表格**：临时 Node 脚本必须在执行结果中记录每个资源的 `action`、`id`、`name`、`resourceType`、`parentId`、`parentName`、`parentFullPath`、`parentInternational`、`international`、`router`、`devVersion`、`sort`、`webShowFlag`、`mobileFlag`。
- **新增后必须补齐父级信息**：脚本执行完必须再次按父级查询资源列表，补齐父级名称、父级完整面包屑路径和父级 `international`，确保最终回执可表格化。

### 5. 静默执行与痕迹清理
1. 脚本创建完成后，使用 `run_command` 工具在终端静默执行该脚本：`node <绝对路径>/run-menu.js`。
2. **分析结果**：
   - 捕获脚本的 `console.log` 返回内容。
   - 如果遇到 `401 Unauthorized`，向用户报告 Token 可能已过期，需重新复制。
   - 如果新增成功，必须按“执行后结果回执表格式要求”输出菜单资源生成结果表；禁止只输出一句捷报或 bullet list。
   - 如果存在前端权限替换，必须同步输出前端权限同步结果表。
3. **清理战场**：
   - 使用 `run_command`（Windows下使用 `Remove-Item -Force <绝对路径>/run-menu.js`）删除临时脚本文件，保持本地目录整洁。

> **警告**：本技能仅限作为接口调用的自动化载体，绝对禁止在此过程中自行发散去修改 Vue 或 TS 业务代码。
